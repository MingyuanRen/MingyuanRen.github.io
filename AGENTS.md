# Publishing approval

For website changes, implement and test locally first, then provide the local
preview for the user to review. Ask for explicit approval before committing.
Do not commit, push, or deploy until the user approves the current changes.
